module: 'BitModule'
# clients
# client: get_config(client.manager)
client: {}

network: nobunaga
block: null

blocks_behind_sync_threshold: 1000

wallet: 
  name: default
  hotkey: default

actor:
  name: local_copy(module)
  refresh: False
