
module: client.manager.ClientManager
client:
  # mongo: get_cfg(client.mongo.manager)
  # minio: get_cfg(client.minio.manager)
  # postgres: get_cfg(client.postgres.manager)
  # ipfs: get_cfg(client.ipfs.manager)
  # graphql: get_cfg(client.graphql.manager)
  # api: get_cfg(client.api.manager)
  local: get_cfg(client.local.manager)
  # ray: get_cfg(client.ray.manager)
