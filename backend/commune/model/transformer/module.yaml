module: 'TransformerModule'

model:
  module: transformers.AutoModelForTokenClassification
  fn: from_pretrained
  kwargs: 
    pretrained_model_name_or_path: distilbert-base-uncased
    # hf_token: local_copy(hf_token)
  
tokenizer:
  module: transformers.AutoTokenizer
  fn: from_pretrained
  kwargs: 
    pretrained_model_name_or_path: distilbert-base-uncased
    # hf_token: local_copy(hf_token)
  
hf_token: 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