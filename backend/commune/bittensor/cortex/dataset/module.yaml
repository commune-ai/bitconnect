module: 'DatasetModule'

client: {}


# submodule:
#   queue: 
#     module: ray.server.queue.QueueServer
#     actor : {refresh: False}

queue: 
  module: ray.server.queue
  actor : {refresh: False}

dataset:
  module: huggingface.dataset
  actor: {'refresh': False}


num_endpoints: 50

synapses: ['TextLastHiddenState']

receptor_pool:
  path: bittensor.receptor.pool
  params:
    max_worker_threads: 0
    max_active_receptors: 0
  
  actor: {'refresh': False}


  memory_threshold:
    value: 0.1
    mode: ratio

  replicas: null
  actor: False

# network: nobunaga
network: nakamoto
block: null
blocks_behind_sync_threshold: 1000


wallet: 
  name: const
  hotkey: Tiberius


actor:
  name: local_copy(module)
  refresh: False
  resources: {num_cpus: 1.0}

