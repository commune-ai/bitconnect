module: 'BitModule'
# clients
# client: get_config(client.manager)


network: nobunaga
block: null

blocks_behind_sync_threshold: 100

wallet: 
  name: default
  hotkey: default

actor:
  name: local_copy(module)
  refresh: False
